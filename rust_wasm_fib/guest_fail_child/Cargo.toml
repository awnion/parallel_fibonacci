[package]
name = "rust_wasm_fib_guest_fail_child"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
runtime = { package = "rust_wasm_fib_runtime", path = "../runtime" }
wit-bindgen.workspace = true
